<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Redirect everything to the dist folder
  RewriteRule ^$ dist/ [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ dist/$1 [L]
</IfModule>